/*
 *
        } else if (strcmp(args[i], "fork") == 0) {
            pid_t pid = fork();
            if (pid == 0) {
                printf("This is the child process.\n");
                exit(EXIT_SUCCESS);
            } else if (pid > 0) {
                printf("This is the parent process.\n");
            } else {
                perror("lsh");
            }

